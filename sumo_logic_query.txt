_sourceCategory="webapp/logs"
| json field=_raw "endpoint" as api_endpoint
| json field=_raw "response_time" as duration
| where api_endpoint = "/api/data"
| where duration > 3000
| timeslice 10m
| count as slow_requests by _timeslice
| where slow_requests > 5
